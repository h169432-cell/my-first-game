# Game Design Document

## 1. Project Goal
「クルーバージュ」の推理システムとゲーム進行をブラウザ上で再現する。

まずは1台のPC/スマホを使って遊べるローカル対戦版を作り、その後必要に応じてオンライン対戦へ拡張する。

> 公開・配布する場合は、元作品のロゴ・カード画像・説明書文章などの表現物はそのまま使用せず、オリジナル素材に置き換える。

## 2. Target
- 1〜4人
- ブラウザで動作
- PCを第一対象、スマホにも対応
- インストール不要
- 1ゲーム約5〜20分を想定

## 3. Core Concept
16枚のカードを4×4にランダム配置し、一部の情報だけを見ながら真犯人を推理する。

真犯人候補になる基本条件は以下。

1. 動機を持っている
2. 有効なアリバイを持っていない
3. 犯行に使用された有効な凶器から指されている

条件を満たす容疑者が複数いる場合は、原則として番号が最も大きい容疑者を真犯人とする。
「どんでん返し」が有効な場合は番号が最も小さい容疑者を真犯人とする。
候補者が0人なら「国外逃亡」とする。

## 4. Card Set
基本カード15枚 + 特殊カード1枚 = 16枚。

### Suspect / 容疑者
- 容疑者1〜6
- 特殊カードによって容疑者7が追加される場合がある

### Motive / 動機
- 2枚
- 指定方向の直線上にいる容疑者へ「動機あり」を付与する

### Alibi / アリバイ
- 2枚
- 指定方向の直線上にいる容疑者へ「アリバイあり」を付与する

### Weapon / 凶器
- 2枚
- 指定方向の直線上にいる容疑者を犯人候補として指す
- ただし手掛かりによって有効化されている凶器のみ使用する

### Clue / 手掛かり
- 2枚
- 周囲に隣接している凶器を有効化する

### False Testimony / 偽証
- 1枚
- 隣接する動機・アリバイを無効化する
- 特殊カードによって追加の偽証が入る場合がある

### Scene Investigation / 現場捜査カード
ゲームごとに以下の候補から1枚を使用する。

- 容疑者7
- 偽証
- どんでん返し

## 5. Board
- 4列 × 4行
- 合計16マス
- ゲーム開始時に16枚をランダム配置
- 各カードは表向き / 裏向きの状態を持つ
- プレイヤーごとに「自分だけ確認済み」の情報を持てるようにする

## 6. Direction Logic
方向効果はカードの位置を起点とし、指定された方向の直線上を対象とする。

例:
- ↑ : 同じ列で上側にあるカード
- ↓ : 同じ列で下側にあるカード
- ← : 同じ行で左側にあるカード
- → : 同じ行で右側にあるカード

距離は問わず、指定方向上にいる対象カードすべてを判定対象とする。

## 7. Adjacency Logic
「隣接」は原則として上下左右の隣接マスとして実装する。

- 手掛かり → 隣接する凶器を有効化
- 偽証 → 隣接する動機・アリバイを無効化

実物ルール確認時に斜め隣接が含まれることが判明した場合はここを修正する。

## 8. Culprit Calculation
ゲーム内部では、すべてのカード配置から真犯人を自動計算する。

### Step 1: 有効/無効カードを確定
- 偽証に隣接する動機を無効化
- 偽証に隣接するアリバイを無効化

### Step 2: 有効な凶器を確定
- 手掛かりに隣接する凶器を有効化
- それ以外の凶器は犯人判定に使用しない

### Step 3: 各容疑者の状態を計算
各容疑者について以下を計算する。

- hasMotive
- hasAlibi
- pointedByActiveWeapon

### Step 4: 候補者抽出
以下をすべて満たす容疑者を候補者とする。

`hasMotive && !hasAlibi && pointedByActiveWeapon`

### Step 5: 真犯人決定
- 候補0人 → 国外逃亡
- 候補1人 → その容疑者
- 候補2人以上 → 最大番号の容疑者
- どんでん返し有効 → 最小番号の容疑者

## 9. Player State
各プレイヤーは以下を持つ。

- playerId
- name
- color
- pawnPosition
- privateKnownCards[]
- accusation
- roundWins
- hasAccused

## 10. Round Setup
1. 基本カード15枚を用意
2. 現場捜査カードを1枚ランダムに追加
3. 16枚をシャッフル
4. 4×4に配置
5. プレイヤー人数を確定
6. 初期公開カードを設定
7. 各プレイヤーのポーンを初期状態にする
8. ゲーム内部で真犯人を計算

※プレイヤー人数ごとの正確な初期公開位置は、実物ルール/事件カードを確認後にデータ化する。

## 11. Turn Flow
### 初回手番
- 伏せカードを1枚選択
- 自分のポーンをその位置へ置く
- そのカード内容を自分だけ確認する

### 2回目以降
プレイヤーは以下から行動を選ぶ。

#### A. 移動して調査
1. 別の伏せカードを選ぶ
2. 新しい位置へポーン移動
3. 新しいカードを自分だけ確認
4. 元いたカードを全員に公開

#### B. 公開調査
- ポーンを動かさず、別の伏せカードを1枚全員に公開する

#### C. 犯人確保
- 容疑者1〜7、または国外逃亡を選択
- 回答を確定
- 以後そのラウンドの通常調査には参加しない

## 12. Round End
全プレイヤーが回答したらラウンド終了。

1. 残りのカードをすべて公開
2. 真犯人判定ロジックを表示
3. 各プレイヤーの回答と照合
4. 正解者に1勝を付与
5. 次ラウンドへ

## 13. Match End
最初に2勝したプレイヤーをゲーム全体の勝者とする。

複数人が同じラウンドで2勝に到達した場合の扱いは、実物ルール確認後に確定する。

## 14. Screens
### Title
- ゲーム開始
- ルール説明
- プレイヤー人数選択

### Player Setup
- 1〜4人選択
- プレイヤー名入力

### Game Board
- 4×4カード盤面
- 表/裏カード表示
- 現在の手番
- 各プレイヤーの勝利数
- 調査 / 公開 / 犯人確保 UI

### Private Inspection
1台端末で遊ぶための「本人だけ見る」画面。

1. 次のプレイヤー名を表示
2. 「確認する」を押す
3. 対象カードを表示
4. 「隠す」を押してゲーム画面へ戻る

### Accusation
- 容疑者1〜7
- 国外逃亡
- 回答確定ボタン

### Result
- 真犯人
- 判定根拠
- 各プレイヤーの回答
- 正解者
- 現在の勝利数
- 次のラウンド

## 15. UX Requirement
真犯人発表時には、単に答えだけ出すのではなく判定理由を視覚化する。

例:
- 動機あり → 強調表示
- アリバイあり → 除外表示
- 偽証で無効 → 打ち消し表示
- 有効な凶器 → 強調表示
- 凶器が指す容疑者 → 矢印表示

これにより、ボードゲームの答え合わせの楽しさを再現する。

## 16. Data Model
カード例:

```js
{
  id: 'motive-1',
  type: 'motive',
  directions: ['up', 'right'],
  faceUp: false
}
```

容疑者例:

```js
{
  id: 'suspect-3',
  type: 'suspect',
  suspectNumber: 3,
  faceUp: false
}
```

## 17. Technical Design
初期版:

- HTML
- CSS
- Vanilla JavaScript
- GitHub Pagesで公開可能な構成

予定ファイル:

- `index.html`
- `style.css`
- `game.js`
- `cards.js`
- `culprit.js`
- `DESIGN.md`

### responsibilities
- `cards.js` : カード定義・デッキ生成
- `culprit.js` : 方向・隣接・真犯人判定
- `game.js` : 手番、プレイヤー、画面遷移

## 18. MVP
最初の実装ではオンライン通信を入れず、1端末で完結させる。

MVP条件:
- 1〜4人を選べる
- 16枚をランダム配置できる
- カードを表/裏で管理できる
- 自分だけカードを確認できる
- 調査・公開・犯人確保ができる
- 真犯人を自動判定できる
- 結果と判定理由を表示できる
- 2勝でゲーム終了できる

## 19. Phase 2
MVP完成後の候補:

- オンライン対戦
- ルーム作成
- スマホ最適化
- 効果音
- アニメーション
- 事件カード/初期公開パターン追加
- 対戦履歴

## 20. Before Coding
実装開始前に追加確認したい点:

1. 各カードに実際に描かれている矢印方向
2. 手掛かり・偽証の正確な隣接範囲
3. プレイヤー人数別の初期公開カード位置
4. 事件カードの全パターン
5. 同時に2勝へ到達した場合の処理

上記のうち確認できない部分は、初期版では仮ルールとして実装し、後から差し替え可能なデータ構造にする。
